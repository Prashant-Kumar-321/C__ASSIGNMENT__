#include<stdio.h>
#include<conio.h>

int main()
{
  // using loop instruction to calcualte sum of first n natural number
  int n,sum = 0 ;

  printf("\t\t\t\tSum of N Natural Number\n\t\t\t\t***********************\n ");
  printf("Enter value of n = ");
  scanf("%d",&n);

  for (int i = 1 ; i <= n ; i++)
  {
   sum += i ;
   printf("%d + ",i);
  }
  printf("  = %d",sum);

  printf("\n\nsum of first %d natural number is %d\n\n",n,sum);
  getch();

  // program to print sum of n natural number without using loop // using mathematical formula n *(n+1) / 2
   printf("\n\nsum of first %d natural number = %d ",n,(n*(n+1)/2));
  return 0;
}

















































































































































































































































































































